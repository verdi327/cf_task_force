$(".pages.wod").ready(function() {
	// $("a#btn-loader[data-remote]").on("ajax:beforeSend", function(e, data, status, xhr) {
	//   $(this).prepend("<i class='fa fa-refresh fa-spin'></i>")}
	// ).on("ajax:complete", function(e, data, status, xhr){
	// 	$(this).html("See More Wods");
	// 	$("#reset-link").show();
	// })
});